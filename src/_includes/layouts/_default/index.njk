---
title: BryceWray.com
layout: layouts/_default/base.njk
---

<main>
  <div class="px-8 text-center mr-auto ml-auto lg:w-full lg:flex lg:flex-row lg:p-0 lg:m-0">
    <div class="lg:text-right lg:pl-4 lg:pr-12 lg:text-white lg:bg-gradient-to-b lg:from-blue-700 lg:to-black lg:dark:from-black lg:dark:to-blue-700 lg:dark:black lg:w-5/12">
      <h1 class="text-xl lg:text-3xl 2xl:text-4xl pt-6 lg:pt-24 2xl:pt-28 tracking-tight lg:leading-relaxed">Welcome to brycewray.com, offering <a href="/about" class="lg:text-blue-100 lg:border-blue-100">its owner</a>&rsquo;s &ldquo;observations, opinions, and geekiness&rdquo; since&nbsp;2018.</h1>
    </div>
    <div class="mr-auto ml-auto border-b-4 border-blue-700 dark:border-white w-4/5 lg:w-7/12 mt-8 mb-8 md:w-1/3 2xl:w-1/4 lg:hidden"></div>
    <div class="lg:text-left lg:pt-16 lg:pr-6 lg:pl-16 lg:w-3/5 2xl:w-2/3 mr-auto ml-auto">
      <h2 class="h1 text-4xl lg:text-5xl 2xl:text-6xl mb-8 2xl:mb-16">Latest <a href="/posts/">posts</a></h2>
      {% for post in collections.post | reverse %}
        {% if loop.index0 < 3 %}	
          <div>
            <h2 class="h4 text-2xl lg:text-3xl 2xl:text-4xl tracking-tight mb-2 2xl:mb-4"><a href="{{ post.url }}">{{ post.data.title | safe }}</a></h2>
            <p class="font-bold text-xl lg:text-2xl 2xl:text-3xl mt-1 mb-0 leading-6">{{ post.data.subtitle | safe }}</p>
            <p class="text-xs lg:text-sm 2xl:text-base mt-1 mb-0"><strong><time style="display: inline;" datetime="{{ post.date | htmlDateString }}">{{ post.date | htmlDateString }}</time></strong>
              {% if post.data.lastmod %}
              <br />Last modified <time datetime="{{ post.date | htmlDateString }}">{{ post.data.lastmod | htmlDateString }}</time>
              {% endif %}
            </p>
            <p class="text-sm lg:text-base 2xl:text-xl mt-1 mb-10 2xl:mb-20 leading-5">
              {{ post.data.description | safe }}
            </p>
          </div>
        {% endif %}
      {% endfor %}
      <p class="mt-8 mb-8 2xl:mb-12 2xl:text-xl"><a href="/posts/"><strong>All {{ collections.post.length }} posts</strong></a> <span class="text-sm 2xl:text-base"><em>(listed five per page)</em></span></p>
    </div>
  </div>
</main>